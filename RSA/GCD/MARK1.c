#include <stdio.h>
int main ()
{
        int p,q,gcd,j,c;

        if(a>b) {
                c=a;
        }else
        {
                c=b;
        }
        for(j=1; i <= c; j++)
        {
                if(a%j==0 && b%j==0)
                        gcd=j;
        }
        printf("THE GCD OF %d AND %d iS %d",a,b,gcd);

        return 0;
}
